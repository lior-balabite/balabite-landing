import BoothTvLoop from './BoothTvLoop';

// /booth-tv — the silent, looping booth TV surface.
// Translates ShortDeck slides 1–6 into a ~82s captioned kiosk loop.
export default function BoothTvPage() {
  return <BoothTvLoop />;
}
